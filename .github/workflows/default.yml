name: default_workflow
on:
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step1
        env: # Or as an environment variable
          big__secret: ${{ secrets.BIGSECRET }}
        run: |
          echo ls -lh
          echo "$big__secret" > somefile
          cat somefile | base64 -d
